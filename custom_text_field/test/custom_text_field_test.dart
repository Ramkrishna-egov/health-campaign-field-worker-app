import 'package:custom_text_field/custom_text_field.dart';
import 'package:test/test.dart';

void main() {
  group('A group of tests', () {
    final awesome = CustomTextFormField(
      label: 'Label',
      formControlName: '',
    );

    setUp(() {
      // Additional setup goes here.
    });

    test('First Test', () {});
  });
}
